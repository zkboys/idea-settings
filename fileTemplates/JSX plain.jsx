import React from 'react';

class ${NAME} extends React.Component {
    state = {};
    static defaultProps = {};
    static propTypes = {};

    componentWillMount() {
    };

    render() {
        return (
            <div>render</div>
        );
    };
}
export default ${NAME};
